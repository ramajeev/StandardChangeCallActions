name: Caller Workflow

on:
  workflow_dispatch:

jobs:
  call-test-secrets:
  #.github/workflows/
    uses: ramajeev/StandardChangeCallActions/.github/workflows/TestingSecretsWF.yml@main
    with:
      example_input: "your_value_here"
    

